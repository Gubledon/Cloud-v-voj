apiVersion: v1
kind: Service
metadata:
  name: java-app
  namespace: sb-demo
  labels:
    app: java-app
spec:
  type: LoadBalancer
  selector:
    app: java-app
  ports:
    - port: 8080
      targetPort: 8080
